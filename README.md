.

💬 How to Use the Chat

بعد تشغيل السيرفر (Laravel) والكلينت (Next.js)، اتبع الخطوات التالية لاستخدام الشات:

افتح المتصفح وروّح للعنوان:
http://localhost:3000

هتلاقي صفحة الدخول أو تسجيل المستخدم (لو موجود).

سجّل الدخول أو ادخل باسم مستخدم لو الشات مفتوح بدون تسجيل.

بعد الدخول، هتظهر لك واجهة الشات، واللي فيها:

قائمة المستخدمين أو الغرف (لو عندك نظام rooms).

منطقة الرسائل.

مربع إرسال الرسائل تحت.

ابعت رسالة عن طريق كتابة النص في مربع الكتابة ثم اضغط Send.

هتشوف الرسائل بتتحدث مباشرة Real‑Time باستخدام:

Laravel + Pusher

Next.js + Tailwind

لو المستخدم التاني فاتح الشات في نفس اللحظة، هتوصل له الرسالة فورًا بدون ما يعمل Refresh.

تقدر تفتح متصفحين مختلفين (مثلاً Chrome + Firefox) لتجربة استقبال الرسائل بين مستخدمين مختلفين.
